# docker-compose.pipeline.yml
#
# Extends docker-compose.yml with Postgres + Airflow.
# JARs are built locally and mounted from ./pipeline/jars/
#
# Airflow uses its own dedicated database (airflow_db) to avoid
# mixing its internal tables with the estate data (sreality database).

networks:
  scraper-net:
    name: scraper-shared-net
    driver: bridge

volumes:
  pg-data:
  airflow-logs:

x-airflow-env: &airflow-env
  AIRFLOW__CORE__EXECUTOR: LocalExecutor
  # Airflow uses its own database so its tables never mix with estate data
  AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://${PG_USERNAME:-sreality}:${PG_PASSWORD:-changeme}@postgres:5432/airflow_db
  AIRFLOW__CORE__FERNET_KEY: ${AIRFLOW_FERNET_KEY:-}
  AIRFLOW__WEBSERVER__SECRET_KEY: ${AIRFLOW_SECRET_KEY:-changeme-secret}
  AIRFLOW__CORE__LOAD_EXAMPLES: "false"
  # Reverse proxy (Caddy) terminates TLS and forwards as HTTP; tell Airflow
  # to honor X-Forwarded-* headers so redirects, cookie scope and links use HTTPS.
  AIRFLOW__WEBSERVER__BASE_URL: ${AIRFLOW_BASE_URL:-http://localhost:8080}
  AIRFLOW__WEBSERVER__ENABLE_PROXY_FIX: "True"
  AIRFLOW__WEBSERVER__COOKIE_SECURE: "True"
  AIRFLOW__SCHEDULER__MAX_DAGRUNS_TO_CREATE_PER_LOOP: "2"
  AIRFLOW__SCHEDULER__PARSING_PROCESSES: "1"
  AIRFLOW__CORE__PARALLELISM: "4"
  AIRFLOW__CORE__MAX_ACTIVE_TASKS_PER_DAG: "2"
  # Pipeline connection vars inherited by all tasks
  MONGO_HOST: mongodb
  MONGO_PORT: "27017"
  MONGO_DATABASE: ${MONGO_DATABASE:-sreality}
  MONGO_USERNAME: ${MONGO_USERNAME:-scraper}
  MONGO_PASSWORD: ${MONGO_PASSWORD:-changeme}
  PG_HOST: postgres
  PG_PORT: "5432"
  PG_DATABASE: ${PG_DATABASE:-sreality}
  PG_USERNAME: ${PG_USERNAME:-sreality}
  PG_PASSWORD: ${PG_PASSWORD:-changeme}
  PG_SCHEMA: ${PG_SCHEMA:-public}
  SREALITY_BASE_URL: ${SREALITY_BASE_URL:-https://www.sreality.cz/api/cs/v2/estates}
  REQUEST_DELAY_MS: ${REQUEST_DELAY_MS:-500}
  TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
  TELEGRAM_CHAT_ID: ${TELEGRAM_CHAT_ID:-}
  CSU_XLSX_URLS: ${CSU_XLSX_URLS:-}
  CSU_YEAR: ${CSU_YEAR:-2024}

x-airflow-volumes: &airflow-volumes
  - ./pipeline/airflow/dags:/opt/airflow/dags:ro
  - airflow-logs:/opt/airflow/logs
  - ./pipeline/jars:/jars:ro

services:

  # ---------------------------------------------------------------------------
  # Postgres — hosts both the estate data (sreality db) and Airflow metadata
  # (airflow_db). Two databases, one container — saves RAM on the small server.
  # Schema applied automatically on first start via postgres-init/01-schema.sh
  # ---------------------------------------------------------------------------
  postgres:
    image: postgis/postgis:16-3.4-alpine
    container_name: sreality-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB:       ${PG_DATABASE:-sreality}
      POSTGRES_USER:     ${PG_USERNAME:-sreality}
      POSTGRES_PASSWORD: ${PG_PASSWORD:-changeme}
    volumes:
      - pg-data:/var/lib/postgresql/data
      - ./postgres-init:/docker-entrypoint-initdb.d:ro
    networks:
      - scraper-net
    ports:
      - "127.0.0.1:${PG_HOST_PORT:-5433}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${PG_USERNAME:-sreality} -d ${PG_DATABASE:-sreality}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    mem_limit: 300m

  # ---------------------------------------------------------------------------
  # Airflow — custom image with Java 21 JRE
  # ---------------------------------------------------------------------------
  airflow-init:
    build:
      context: .
      dockerfile: Dockerfile.airflow
    container_name: sreality-airflow-init
    restart: "no"
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      <<: *airflow-env
      _AIRFLOW_DB_MIGRATE: "true"
      _AIRFLOW_WWW_USER_CREATE: "true"
      _AIRFLOW_WWW_USER_USERNAME: ${AIRFLOW_USER:-admin}
      _AIRFLOW_WWW_USER_PASSWORD: ${AIRFLOW_PASSWORD:-admin}
    volumes: *airflow-volumes
    command: version
    networks:
      - scraper-net

  airflow-webserver:
    build:
      context: .
      dockerfile: Dockerfile.airflow
    container_name: sreality-airflow-webserver
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment: *airflow-env
    volumes: *airflow-volumes
    ports:
      - "127.0.0.1:${AIRFLOW_PORT:-8080}:8080"
    command: webserver
    networks:
      - scraper-net
    mem_limit: 400m
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  airflow-scheduler:
    build:
      context: .
      dockerfile: Dockerfile.airflow
    container_name: sreality-airflow-scheduler
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment: *airflow-env
    volumes: *airflow-volumes
    command: scheduler
    networks:
      - scraper-net
    mem_limit: 500m
