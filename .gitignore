# Maven
target/
*.class

# IDE
.idea/
*.iml
.vscode/

# Environment / secrets – NEVER commit the real .env
.env

# Logs
logs/

# OS
.DS_Store
Thumbs.db

# Pipeline JARs — built by GitHub Actions, copied to server via SCP
# The jars/ directory itself is tracked (via .gitkeep) but not the JARs
pipeline/jars/*.jar
initial-load/target/
.claude/
.venv/
