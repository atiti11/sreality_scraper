# DEPRECATED — this file is no longer copied into the container.
#
# The nginx config now lives in ``default.conf.template`` and is rendered
# at container startup so the ``${API_KEY}`` placeholder can be substituted
# from the environment. See the Dockerfile for details.
