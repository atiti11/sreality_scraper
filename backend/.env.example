# This file is intentionally empty. The project now uses a single
# ``.env.example`` at the repo root — copy that to ``.env`` and fill in
# values once. Both docker-compose and python-dotenv pick the root file up
# automatically (the latter walks up the directory tree from cwd).
